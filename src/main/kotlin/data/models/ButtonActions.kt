package data.models

/**
 * @author Aidan Scott
 * ButtonActions holds button values. if another value is added another button will be added.
 * Functionality must be added manually by extending if statement in ViewColumn.kt
 */
enum class ButtonActions {
    CAPSULE, TANK, ENGINE, LAUNCH, HELP
}